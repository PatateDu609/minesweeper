#ifndef UTILS_H
#define UTILS_H

#include <string.h>
#include <stdlib.h>

int *sort(int *arr, int len);
char *strjoin(const char *, const char *);
int *find_sorted(int *arr, int len, int val);

#endif
